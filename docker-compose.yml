services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: goqueue
      POSTGRES_USER: goqueue
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U goqueue"]
      interval: 5s
      retries: 5
